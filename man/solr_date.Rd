% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/solr-date.R
\name{solr_date}
\alias{solr_date}
\title{Parse a Solr date, Date Math expression, or range}
\usage{
solr_date(x)
}
\arguments{
\item{x}{A scalar input to parse. Supported inputs are:
\itemize{
\item an existing \code{SolrDate} object, which is returned unchanged;
\item a scalar \code{Date} or \code{POSIXt} object;
\item a scalar numeric value, which is first converted to character;
\item a scalar character string representing either a single boundary or a
complete Solr range expression.
}

\code{POSIXt} inputs must use the \code{"UTC"} timezone.}
}
\value{
An internal S7 object inheriting from \code{SolrDate}. The exact
subclass is an implementation detail and may represent a single instant,
a Date Math expression, an unbounded boundary, or a range.
}
\description{
\code{solr_date()} parses a scalar input into an internal S7 \code{SolrDate} object.
The resulting object can represent a single instant, a Date Math expression,
an unbounded boundary (\code{*}), or a Solr range.
}
\details{
Character inputs support the following forms:
\itemize{
\item Simplified dates such as \code{"2025"}, \code{"2025-02"}, \code{"2025-02-03"}, and
\code{"20250203"}.
\item Datetimes accepted by the internal parser, including ISO-like forms such as
\code{"2025-01-15T12:30:45Z"}, timezone offsets like \code{"+08:00"}, and common
separators such as \code{"/"} and \code{"."}.
\item Solr Date Math expressions rooted at \code{NOW}, e.g. \code{"NOW"},
\code{"NOW-1YEAR"}, or \code{"NOW/DAY-1YEAR+6MONTHS"}.
\item Fixed-base Date Math expressions of the form
\code{"<datetime>Z<math>"}, e.g. \code{"2025-01-01T00:00:00Z+1MONTH"}.
\item Solr range expressions using the exact separator \code{" TO "} and boundary
brackets \verb{\[\]} or \verb{\\\{\\\}}, e.g. \verb{"\[2000 TO 2010\]"}, \verb{"\\\{2000 TO 2010\]"}, or
\verb{"\[* TO *\]"}.
}

Supported Date Math operators are \code{+}, \code{-}, and \code{/}. Supported units are
\code{YEAR}, \code{YEARS}, \code{MONTH}, \code{MONTHS}, \code{DAY}, \code{DAYS}, \code{DATE}, \code{HOUR}, \code{HOURS},
\code{MINUTE}, \code{MINUTES}, \code{SECOND}, \code{SECONDS}, \code{MILLI}, \code{MILLIS}, \code{MILLISECOND},
and \code{MILLISECONDS}.

Use \code{format()} or \code{as.character()} to render a parsed value. \code{format()}
supports \code{as = "iso"} and \code{as = "num"}. \code{as.POSIXct()} can be used on
instants; for ranges it returns the start boundary with a warning, and for
unbounded or Date Math values it errors because no single concrete instant is
available.
}
\examples{
solr_date("2025")
solr_date("2025-02")
solr_date("20250203")
solr_date("2025-01-15T12:30:45Z")

solr_date("NOW")
solr_date("NOW/DAY-1YEAR+6MONTHS")
solr_date("2025-01-01T00:00:00Z+1MONTH")

solr_date("[2000 TO 2010]")
solr_date("{2000 TO 2010]")
solr_date("[* TO *]")

x <- solr_date("2025-01-15T12:30:45Z")
format(x)
format(x, as = "num")
as.character(x)
as.POSIXct(x)
is.solr_date(x)
print(x)

}
\seealso{
\code{\link[=is.solr_date]{is.solr_date()}}
}
