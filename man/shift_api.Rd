% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shift-stage.R, R/shift-ui.R
\name{shift_complete}
\alias{shift_complete}
\alias{shift_api}
\alias{shift_request}
\alias{shift_site}
\alias{shift_cmip6}
\alias{shift_control}
\alias{shift_cmip6_scenario}
\alias{shift_reference_plan}
\alias{historical_reference}
\alias{shift_plan}
\alias{shift_future_epw}
\alias{shift_reference_historical}
\alias{shift_collect}
\alias{shift_download}
\alias{shift_extract}
\alias{shift_morph}
\alias{shift_epw}
\alias{shift_explain}
\alias{shift_run}
\alias{shift_export_epw}
\alias{shift_check}
\alias{shift_refresh}
\alias{shift_ids}
\alias{shift_cases}
\alias{shift_missing}
\alias{shift_runs}
\alias{shift_run_get}
\alias{shift_result}
\alias{shift_resume}
\alias{shift_watch}
\alias{shift_cancel}
\alias{shift_logs}
\alias{shift_datasets}
\alias{shift_files}
\alias{shift_data}
\alias{shift_diagnostics}
\alias{shift_store}
\alias{shift_target}
\alias{shift_coverage}
\alias{shift_outputs}
\alias{shift_artifacts}
\alias{shift_status}
\alias{shift_ui}
\title{Store-native shift workflow API}
\usage{
shift_complete(x)

shift_request(
  provider = "esgf",
  project = NULL,
  source = NULL,
  experiment = NULL,
  variant = NULL,
  variables = NULL,
  frequency = NULL,
  time = NULL,
  filters = list(),
  options = list(),
  ...
)

shift_site(
  id = NULL,
  lon = NULL,
  lat = NULL,
  label = NULL,
  epw = NULL,
  metadata = list()
)

shift_cmip6(
  model = 3L,
  scenarios,
  member = NULL,
  grid = NULL,
  frequency = NULL,
  table = NULL,
  activity = "ScenarioMIP",
  index_nodes = NULL,
  data_node = NULL,
  filters = list()
)

shift_control(
  strict = TRUE,
  allow_partial = FALSE,
  download = c("auto", "always", "never"),
  resume = TRUE,
  overwrite = FALSE,
  extraction_method = "nearest",
  output_layout = c("nested", "flat")
)

shift_cmip6_scenario(
  source,
  scenario,
  member = NULL,
  years = NULL,
  variables = "recommended",
  frequency = "mon",
  activity = "ScenarioMIP",
  table_id = NULL,
  grid_label = NULL,
  data_node = NULL,
  index_node = NULL,
  filters = list(),
  options = list()
)

shift_reference_plan(
  plan_id,
  periods,
  role = c("model_historical", "observed_reference")
)

historical_reference(years = 1995:2014, period = "reference", ...)

shift_plan(
  request,
  site,
  periods,
  store,
  transform,
  reference = NULL,
  observed_reference = NULL,
  control = shift_control(),
  collect = list(),
  download = list(),
  extract = list(),
  morph = list(),
  epw = list()
)

shift_future_epw(
  epw,
  climate,
  periods,
  transform = NULL,
  dir,
  reference = NULL,
  observed_reference = NULL,
  control = shift_control(),
  ui = shift_ui(),
  store = NULL,
  dry_run = FALSE,
  background = FALSE,
  methods = NULL,
  calibration = NULL
)

shift_reference_historical(
  periods,
  experiment = "historical",
  activity = "CMIP",
  match = c("source_id", "variant_label", "frequency", "table_id", "grid_label"),
  filters = list(),
  options = list(),
  collect = list(),
  extract = list(fallback = "auto")
)

shift_collect(
  x,
  store = NULL,
  fields = "*",
  all = TRUE,
  limit = FALSE,
  label = NULL,
  ui = NULL,
  ...
)

shift_download(
  x,
  downloader = NULL,
  run = TRUE,
  background = FALSE,
  resume = TRUE,
  overwrite = FALSE,
  session_label = NULL,
  ui = NULL,
  ...
)

shift_extract(
  x,
  site = NULL,
  periods = NULL,
  variables = NULL,
  time = NULL,
  filters = list(),
  method = "nearest",
  fallback = c("auto", "error"),
  overwrite = FALSE,
  resume = TRUE,
  ui = NULL
)

shift_morph(
  x,
  baseline = NULL,
  transform,
  reference = NULL,
  observed_reference = NULL,
  strict = TRUE,
  complete_only = TRUE,
  by = c("source_id", "experiment_id", "variant_label", "period"),
  overwrite = FALSE,
  resume = TRUE,
  ui = NULL
)

shift_epw(
  x,
  dir = NULL,
  separate = TRUE,
  export_dir = NULL,
  overwrite = FALSE,
  resume = TRUE,
  ui = NULL
)

shift_explain(x, ...)

shift_run(x, background = FALSE, ui = shift_ui(), ...)

shift_export_epw(
  x,
  dir,
  separate = TRUE,
  overwrite = FALSE,
  resume = TRUE,
  ui = NULL
)

shift_check(x, strict = FALSE, network = FALSE, ...)

shift_refresh(x)

shift_ids(x, refresh = TRUE)

shift_cases(x, refresh = TRUE)

shift_missing(x)

shift_runs(store = NULL)

shift_run_get(run_id, store = NULL)

shift_result(x, store = NULL)

shift_resume(x, store = NULL, background = FALSE, ui = shift_ui())

shift_watch(
  x,
  store = NULL,
  follow = TRUE,
  interval = 1,
  events = 10L,
  ui = shift_ui()
)

shift_cancel(x, store = NULL, force = FALSE)

shift_logs(x, store = NULL, tail = 100L)

shift_datasets(x, all = TRUE, limit = FALSE, store = NULL, ui = NULL)

shift_files(x)

shift_data(
  x,
  n = 100L,
  variables = NULL,
  case_id = NULL,
  columns = NULL,
  refresh = TRUE
)

shift_diagnostics(x, severity = NULL, refresh = TRUE)

shift_store(x, create = FALSE)

shift_target(x)

shift_coverage(x)

shift_outputs(x, refresh = TRUE)

shift_artifacts(x)

shift_status(x, refresh = TRUE)

shift_ui(
  progress = c("auto", "dynamic", "log", "none"),
  detail = c("normal", "detail", "debug"),
  motion = c("auto", "full", "reduced", "none"),
  refresh = 0.12,
  heartbeat = 10
)
}
\arguments{
\item{x}{A shift stage object.}

\item{provider}{Climate data provider. The first implementation supports
\code{"esgf"}.}

\item{project}{Optional provider project, for example \code{"CMIP6"}.}

\item{source, experiment, variant, frequency}{Provider-neutral request fields.
Values must use the selected provider's controlled vocabulary.
In \code{shift_cmip6()}, an unnamed scalar \code{frequency} applies to every transform
input, while a named character vector assigns one frequency to every
source variable so \verb{3hrPt}, \verb{3hr}, and \code{day} data can be collected together.
In \code{shift_reference_historical()}, \code{experiment} is the historical
reference experiment filter. Values are not translated; for ESGF, use
exact facet values such as \code{project = "CMIP6"} and \code{frequency = "mon"}.}

\item{variables}{Provider-neutral request alias in \code{\link[=shift_request]{shift_request()}}, optional
extraction variables in \code{\link[=shift_extract]{shift_extract()}}, or optional variables to read in
\code{shift_data()}.}

\item{time}{Optional request or extraction time filter. Numeric years such as
\code{2060L} are expanded to the full UTC year; otherwise supply one or two
date-time values accepted by the provider/store.}

\item{filters}{Provider-specific query filters in \code{\link[=shift_request]{shift_request()}}, or
extraction filters in \code{\link[=shift_extract]{shift_extract()}}.}

\item{options}{Provider-specific request options. For ESGF, \code{index_node} and
\code{time_filter_method} are recognized.}

\item{...}{Additional provider-specific filters or workflow options.}

\item{id}{Optional site identifier. If \code{id} is an EPW file path and \code{epw}
is \code{NULL}, it is treated as \code{epw}.}

\item{lon, lat}{Optional site longitude and latitude. Missing values are read
from the EPW LOCATION header when \code{epw} is supplied.}

\item{label}{Optional label recorded with collected File records.}

\item{epw}{A baseline EPW path, internal \code{EpwFile}, or external object
inheriting from \code{"Epw"} in site and task APIs; in \code{\link[=shift_plan]{shift_plan()}}, a named
EPW export option list.}

\item{metadata}{Optional site metadata.}

\item{model}{CMIP6 model selection. A positive whole number selects that
many compatible models deterministically; a character vector selects
explicit source/model IDs; \code{NULL} selects every compatible model. The
default selects three models.}

\item{scenarios}{CMIP6 future scenario experiment IDs.}

\item{member}{CMIP6 variant label, for example \code{"r1i1p1f1"}.}

\item{grid}{Optional single CMIP6 grid label.}

\item{table}{Optional CMIP6 table selection. \code{NULL} automatically maps each
transform input to its native table (including \code{snd} to \code{LImon}); an unnamed
scalar pins every variable to one table; a named character vector
overrides individual variables.}

\item{activity}{CMIP6 activity ID. \code{shift_cmip6_scenario()} defaults to
\code{"ScenarioMIP"} and \code{shift_reference_historical()} defaults to \code{"CMIP"}.}

\item{index_nodes}{Ordered ESGF index nodes used for failover.}

\item{data_node}{Optional ESGF data node filter.}

\item{strict}{If \code{TRUE}, abort when diagnostics contain errors.}

\item{allow_partial}{Whether a task-level run may complete with missing cases.}

\item{download}{Source-data policy in \code{\link[=shift_control]{shift_control()}} (\code{"auto"},
\code{"always"}, or \code{"never"}), or a named download-stage option list in
\code{\link[=shift_plan]{shift_plan()}}.}

\item{resume}{Whether to reuse complete existing downloads, extraction
outputs, morphing results, or EPW outputs.}

\item{overwrite}{Whether to overwrite existing downloads, extraction outputs,
morphing results, or EPW outputs.}

\item{extraction_method}{Grid extraction method.}

\item{output_layout}{Output directory layout.}

\item{scenario}{CMIP6 scenario experiment IDs, for example
\code{"ssp126"} or \code{"ssp585"}.}

\item{years}{Optional years used to constrain the future request time window.}

\item{table_id}{One or more CMIP6 table IDs. If \code{NULL}, the native table is
inferred for each variable's \code{frequency}.}

\item{grid_label}{Optional CMIP6 grid label.}

\item{index_node}{Optional ESGF index node.}

\item{plan_id}{Store extraction plan IDs for manually selected reference
climate data.}

\item{periods}{A period table, usually from \code{\link[=epw_morph_periods]{epw_morph_periods()}}.
\code{\link[=shift_future_epw]{shift_future_epw()}} and \code{\link[=shift_plan]{shift_plan()}} also accept a numeric vector of
target years; each year becomes an independently named output period.}

\item{role}{Semantic role of the plan-backed climate. Use
\code{"observed_reference"} only for an observational extraction plan.}

\item{period}{Reference period name used when constructing periods from
\code{years}.}

\item{request}{A \code{\link[=shift_request]{shift_request()}} object, commonly from
\code{shift_cmip6_scenario()}.}

\item{site}{A \code{shift_site()} object.}

\item{store}{An \link{EsgStore}, store path, or \code{NULL}.}

\item{transform}{A reusable specification from \code{\link[=monthly_transform]{monthly_transform()}},
\code{\link[=daily_transform]{daily_transform()}}, or \code{\link[=hourly_transform]{hourly_transform()}}.}

\item{reference}{Optional \code{ShiftReferenceSpec} or \code{ShiftClimate} stage
containing historical model climate when required by \code{transform}.}

\item{observed_reference}{Optional plan-backed \code{ShiftReferenceSpec} or
\code{ShiftClimate} stage containing observed weather when required by
\code{transform}.}

\item{control}{Workflow controls from \code{\link[=shift_control]{shift_control()}}.}

\item{collect}{Named collection options. Historical reference collection may
use \code{fields}, \code{all}, \code{limit}, \code{label}, and \code{time}; \code{\link[=shift_plan]{shift_plan()}} applies
the same strict field validation to its collection stage.}

\item{extract}{Named extraction options. Historical reference extraction may
use \code{variables}, \code{time}, \code{filters}, \code{method}, and \code{fallback};
\code{\link[=shift_plan]{shift_plan()}} applies the same strict field validation to its extraction
stage.}

\item{morph}{Named morph-stage options. Stage option lists are validated and
cannot override task-level controls or the transform/reference inputs.}

\item{climate}{A complete future-climate specification from \code{\link[=shift_cmip6]{shift_cmip6()}}.}

\item{dir}{In \code{\link[=shift_future_epw]{shift_future_epw()}}, the user-facing delivery directory. In
\code{\link[=shift_epw]{shift_epw()}}, an output directory inside the store root; relative paths
are resolved under the store root.}

\item{ui}{Runtime presentation options from \code{\link[=shift_ui]{shift_ui()}}.}

\item{dry_run}{If \code{TRUE}, return the planned workflow without running it.}

\item{background}{For \code{\link[=shift_download]{shift_download()}}, whether to run queued downloads in
a background job. For task-level run/resume functions, whether to launch a
detached \code{Rscript} worker and return a queued \code{ShiftRun} immediately.}

\item{methods}{One or more unambiguous method keys from
\code{\link[=weather_transforms]{weather_transforms()}}. This high-level form creates a \code{ShiftBatch} across
every selected method and model.}

\item{calibration}{Alias for \code{observed_reference} in high-level multi-method
workflows. It is routed only to methods that accept observational input.}

\item{match}{File metadata fields copied from the future climate stage when
resolving an automatic historical reference.}

\item{fields}{File fields collected from Dataset records. The default
requests all fields and lets the result/store layers preserve and validate
provider response metadata.}

\item{all, limit}{Collection controls passed to \link{EsgQuery} /
\link{EsgResultDataset}. If a numeric \code{limit} is supplied without explicitly
setting \code{all}, it caps the Dataset result count. With \code{all = TRUE}, a
numeric \code{limit} retains the low-level meaning of pagination page size.}

\item{downloader}{Optional \link{Downloader} instance.}

\item{run}{Whether to run queued downloads immediately. Downloading full
NetCDF files is optional for the normal workflow because \code{\link[=shift_extract]{shift_extract()}}
can use OPeNDAP first and only download as a fallback when requested.}

\item{session_label}{Optional download session label.}

\item{method}{Grid extraction method used by \code{\link[=shift_extract]{shift_extract()}}.}

\item{fallback}{Extraction fallback policy.}

\item{baseline}{Optional baseline EPW path or
\code{shift_site()} object containing \code{epw}.}

\item{complete_only}{Whether \code{\link[=shift_morph]{shift_morph()}} should morph only complete
extraction plans when a climate stage also contains failed or incomplete
plans.}

\item{by}{Grouping columns used to create morphing cases.}

\item{separate}{Whether to create separate output directories per morphing case.}

\item{export_dir}{Optional directory outside or inside the store where EPW
files should also be copied for user-facing delivery.}

\item{network}{For a reanalysis source, whether to verify the configured
token against the provider. The default performs local configuration
checks only and never submits a data request.}

\item{refresh}{In \code{\link[=shift_ui]{shift_ui()}}, minimum seconds between visual animation
frames. In \code{ShiftRun} inspectors, whether to reload persisted state first.}

\item{run_id}{Persisted workflow run ID.}

\item{follow}{Whether to continue watching until the run reaches a terminal
status.}

\item{interval}{Polling interval in seconds.}

\item{events}{Number of recent events to display or return.}

\item{force}{If \code{FALSE}, request cancellation at the next safe workflow
boundary. If \code{TRUE}, persist the request and then terminate the recorded
background worker process immediately.}

\item{tail}{Maximum number of trailing execution log lines to return.}

\item{n}{Maximum number of data rows to read. Use \code{Inf} to read all rows.}

\item{case_id}{Optional morphing case IDs to read from morphed or EPW output
stages.}

\item{columns}{Optional data columns to keep.}

\item{severity}{Optional diagnostic severities to keep.}

\item{create}{Whether to create a store when \code{x} is a path.}

\item{progress}{In \code{\link[=shift_ui]{shift_ui()}}, workflow presentation mode: \code{"auto"},
\code{"dynamic"}, \code{"log"}, or \code{"none"}.}

\item{detail}{Presentation detail level. \code{"normal"} shows task progress,
\code{"detail"} adds selection, reuse, and fallback decisions, and \code{"debug"}
also shows full URLs, paths, and low-level transfer context.}

\item{motion}{Dynamic-terminal animation policy: \code{"auto"}, \code{"full"},
\code{"reduced"}, or \code{"none"}. This never changes log, JSON, or workflow data.}

\item{heartbeat}{Minimum seconds between job-liveness updates. Durable
writes are always throttled to at least one second.
Use \code{progress = "log"} for screen readers, reduced-motion use, redirected
output, and stable captured logs.}
}
\value{
A shift stage object.
}
\description{
\verb{shift_*()} functions provide a stage-oriented workflow facade over
\link{EsgQuery}, \link{EsgStore}, \link{Downloader}, and \link{EpwMorpher}. Each step returns a
small S7 stage object that can be printed, inspected, saved, and passed to the
next step without manually passing manifest IDs.
}
