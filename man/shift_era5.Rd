% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/source-era5.R
\name{shift_era5}
\alias{shift_era5}
\title{Use ERA5 as an observed weather reference}
\usage{
shift_era5(
  years,
  product = c("single_levels", "land"),
  variables = NULL,
  frequency = NULL,
  access = c("auto", "arco", "cds"),
  ...
)
}
\arguments{
\item{years}{Calendar years used as the observed reference period.}

\item{product}{ERA5 single-level product. \code{"single_levels"} uses global
ERA5; \code{"land"} uses ERA5-Land.}

\item{variables}{Optional CF variable IDs. \code{NULL} infers them from the
selected weather method.}

\item{frequency}{Optional \code{"hour"}, \code{"day"}, or \code{"mon"} frequency. A named
vector can assign frequencies by CF variable. \code{NULL} infers the contract.}

\item{access}{Data access route. \code{"auto"} prefers the official ARCO-backed
time-series service for supported point requests and otherwise uses the
complete CDS subset service.}

\item{...}{Named provider options retained in the source specification.}
}
\value{
A reusable \code{ShiftReanalysisSpec} containing no credentials.
}
\description{
\code{shift_era5()} describes an ERA5 calibration source. Variables, temporal
frequency, and the target coordinate are normally inferred from the weather
method and baseline EPW when \code{\link[=shift_future_epw]{shift_future_epw()}} builds the run.
Constructing the source neither reads credentials nor contacts CDS. Use
\code{shift_check(source)} to validate local configuration and
\code{shift_check(source, network = TRUE)} to authenticate the configured token
without submitting a data request. Dataset-specific terms are checked by CDS
when retrieval begins and are never accepted by the package.
}
\references{
\href{https://cds.climate.copernicus.eu/datasets/reanalysis-era5-single-levels-timeseries}{Copernicus Climate Data Store ERA5 hourly time-series data}
and the \href{https://cds.climate.copernicus.eu/en/how-to-api}{CDS API setup guide}.
}
\seealso{
\code{\link[=shift_future_epw]{shift_future_epw()}}, \code{\link[=shift_check]{shift_check()}}
}
