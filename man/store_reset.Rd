% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/store.R
\name{store_reset}
\alias{store_reset}
\title{Recreate an epwshiftr store}
\usage{
store_reset(path = store_dir(init = FALSE), backup = TRUE, force = FALSE)
}
\arguments{
\item{path}{Store directory to recreate. Defaults to \code{\link[=store_dir]{store_dir()}}.}

\item{backup}{Whether to move an existing store to a timestamped sibling
before creating the replacement. Default: \code{TRUE}.}

\item{force}{Required when \code{backup = FALSE} would permanently remove an
existing populated store. Default: \code{FALSE}.}
}
\value{
Invisibly, a list containing the new \code{path}, optional \code{backup_path},
previous and current schema versions, and the performed \code{action}.
}
\description{
\code{store_reset()} creates a current-schema store at \code{path}. Existing stores are
moved to a timestamped sibling by default, preserving query snapshots,
downloads, extracted climate data, generated EPWs, and run history for
manual recovery. Permanent removal requires both \code{backup = FALSE} and
\code{force = TRUE}.
}
\details{
This function does not migrate older manifests. Close open \link{EsgStore}
objects and stop active foreground or background jobs before resetting.
}
