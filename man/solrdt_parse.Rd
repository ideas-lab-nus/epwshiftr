% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-solrdt.R
\name{solrdt_parse}
\alias{solrdt_parse}
\title{Parse Solr DateTime String}
\usage{
solrdt_parse(x)
}
\arguments{
\item{x}{A character vector of datetime strings to parse, \code{POSIXt} objects,
or \code{Date} objects. \code{NA} values are not allowed and will cause an
error.}
}
\value{
A \code{SolrDateTime} object, which is a vectorized S3 class containing:
\itemize{
\item \code{start}: POSIXct vector of start datetimes (UTC). For unbounded ranges
(e.g., \verb{[* TO 2025]}), the start will be \code{NA}
\item \code{end}: POSIXct vector of end datetimes (UTC). For unbounded ranges (e.g.,
\verb{[2000 TO *]}), the end will be \code{NA}
\item \code{start_inclusive}: Logical vector, \code{TRUE} if start boundary is inclusive (\code{[})
\item \code{end_inclusive}: Logical vector, \code{TRUE} if end boundary is inclusive (\verb{]})
\item \code{is_range}: Logical vector, \code{TRUE} for range inputs, \code{FALSE} for single datetimes
\item \code{original}: Character vector of original input strings with whitespaces trimmed
}

The \code{SolrDateTime} class supports vectorization and standard vector operations
like subsetting (\code{[}), \code{length()}, \code{c()}, etc.

Use \code{is_solrdt_range(x)} to check which elements are ranges.
Use \code{is_solrdt_start_unbounded(x)} or \code{is_solrdt_end_unbounded(x)} to check for unbounded ranges.

[`)
\itemize{
\item \code{end_inclusive}: Logical vector, \code{TRUE} if end boundary is inclusive (`]: R:\%60)\%0A\%20\%20-\%20\%60end_inclusive\%60:\%20Logical\%20vector,\%20\%60TRUE\%60\%20if\%20end\%20boundary\%20is\%20inclusive\%20(\%60
}
}
\description{
Parse datetime strings in various formats supported by Solr's DateRangeField.
This function handles simplified formats (year, year-month, date) as well as
full ISO-8601 formats with optional Date Math expressions, and range syntax.
}
\details{
The function always returns a \code{SolrDateTime} object for consistency.

All results are returned in UTC timezone.

The function supports the following input formats:
\subsection{Simplified formats (DateRangeField style)}{
\itemize{
\item Year only: \code{"2025"} -> start of 2025 (2025-01-01 00:00:00)
\item Year-Month: \code{"2025-01"} -> start of January 2025 (2025-01-01 00:00:00)
\item Date: \code{"2025-01-15"} -> start of that day (2025-01-15 00:00:00)
\item YYYYMMDD: \code{"20250115"} -> start of that day (2025-01-15 00:00:00)
}
}

\subsection{ISO-8601 formats}{
\itemize{
\item Full datetime: \code{"2025-01-15T12:30:45Z"}
\item With milliseconds: \code{"2025-01-15T12:30:45.123Z"}
\item Lenient format: \code{"2025-1-15T12:30:45Z"} (missing leading zeros)
}
}

\subsection{Date Math expressions (relative to NOW or a base date)}{
\itemize{
\item \code{"NOW"} -> current time
\item \code{"NOW+1YEAR"} -> one year from now
\item \code{"NOW-1DAY"} -> one day ago
\item \code{"NOW/DAY"} -> start of today
\item \code{"2025-01-01T00:00:00Z+5DAYS"} -> 5 days after the base date
\item \code{"NOW/DAY+6MONTHS+3DAYS"} -> 6 months and 3 days from start of today
}
}

\subsection{Range syntax (DateRangeField specific)}{
\itemize{
\item \code{"[2000 TO 2014]"} -> range from 2000 to 2014 (inclusive)
\item \code{"[2000 TO 2014\}"} -> range from 2000 to 2014 (exclusive end)
\item \code{"\{2000 TO 2014]"} -> range from 2000 to 2014 (exclusive start)
\item \code{"[* TO *]"} -> unbounded range (all time)
\item \code{"[2000-01 TO 2014-12]"} -> range with month precision
\item \code{"[2014-05-21T12:00:00Z TO *]"} -> from specific time to unbounded
}

\strong{Supported Date Math operators:}
\itemize{
\item \code{+} : Addition
\item \code{-} : Subtraction
\item \code{/} : Round down to the start of the unit
}
}

\subsection{Supported time units}{
\itemize{
\item \code{YEAR}, \code{YEARS}
\item \code{MONTH}, \code{MONTHS}
\item \code{DAY}, \code{DAYS}, \code{DATE}
\item \code{HOUR}, \code{HOURS}
\item \code{MINUTE}, \code{MINUTES}
\item \code{SECOND}, \code{SECONDS}
\item \code{MILLI}, \code{MILLIS}, \code{MILLISECOND}, \code{MILLISECONDS}
}
}
}
\note{
\itemize{
\item The 'Z' suffix (indicating UTC) is optional for simplified formats but
required for full ISO-8601 formats when not using Date Math.
\item Negative years (BC dates) are supported: \code{"-1500-01-01T00:00:00Z"}
\item All parsed times are returned in UTC timezone.
\item Range syntax uses \code{*} to represent unbounded (infinite) dates, which are
represented as \code{NA} in the start or end fields.
\item \code{NA} values in input are not allowed. Parsing failures will result in an error.
\item \code{POSIXt} input is automatically converted to UTC and wrapped in \code{SolrDateTime}.
}
}
\examples{
\dontrun{
# Simplified formats
solrdt_parse(2024)
solrdt_parse("2025")
solrdt_parse("2025-01")
solrdt_parse("2025-01-15")

# ISO-8601 formats
solrdt_parse("2025-01-15T12:30:45Z")
solrdt_parse("2025-01-15T12:30:45.123Z")

# Date Math
solrdt_parse("NOW")
solrdt_parse("NOW+1YEAR")
solrdt_parse("NOW-1DAY")
solrdt_parse("NOW/DAY")
solrdt_parse("2025-01-01T00:00:00Z+5DAYS")

# Range syntax
solrdt_parse("[2000 TO 2014]")
solrdt_parse("[* TO *]") # Unbounded range
solrdt_parse("[* TO 2025]") # Unbounded start

# Exclusive boundaries
solrdt_parse("{2000 TO 2014]") # Exclusive start
solrdt_parse("[2000 TO 2014}") # Exclusive end

# Vector input
solrdt_parse(c("2025", "2025-06", "NOW+1YEAR"))
solrdt_parse(c("[2000 TO 2014]", "2025"))

# POSIXt input
solrdt_parse(Sys.time())
solrdt_parse(as.POSIXct("2025-01-01", tz = "UTC"))

# Date input
solrdt_parse(as.Date("2025-01-01"))
solrdt_parse(Sys.Date())

# Check for ranges and unbounded
result <- solrdt_parse(c("[* TO 2025]", "2020", "[2000 TO *]"))
is_solrdt_range(result) # c(TRUE, FALSE, TRUE)
is_solrdt_start_unbounded(result) # c(TRUE, FALSE, FALSE)
is_solrdt_end_unbounded(result) # c(FALSE, FALSE, TRUE)
}

}
\keyword{internal}
